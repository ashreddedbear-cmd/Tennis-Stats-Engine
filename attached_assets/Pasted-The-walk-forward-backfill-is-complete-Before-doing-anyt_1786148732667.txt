The walk-forward backfill is complete. Before doing anything else, verify the results — read-only, no code changes, no new runs triggered.

## 1. Backfill sanity check

- Total new rows generated by the backfill, and how many are gradable (`actual_winner_id IS NOT NULL`).
- Actual date range covered (confirm it matches 06/01/2024–present, or report the real range if it differs).
- Confirm no leakage: spot-check a sample of rows to confirm each prediction only used data available before that match's date.

## 2. Tie-break accuracy (8A) — the original 198-row problem

- Report the new total count of graded post-fix `paper_trade` rows where `tieBreakerApplied = true`.
- Report tie-break accuracy on this new, larger sample.
- Compare it against the original baseline figures: 53.5% (tie-break) vs. 66.8% (non-tie-break baseline).
- State plainly: did the soft no-play fix measurably close that gap, or not?

## 3. Recent Form weight gate (8E)

- If `formEloConflict` has been persisted (Ticket 2) on any of the new rows, compare accuracy where `formEloConflict = true` vs. `false`.
- If `formEloConflict` is NOT yet persisted on these rows, say so explicitly — this check may still be blocked on Ticket 2, not the backfill itself. Don't approximate or infer the flag from other fields.

## 4. Model-disagreement cohort (Ticket 4)

- Using the new backfill data, report accuracy for the model-disagreement cohort vs. the agreement cohort.
- Include raw counts and confidence intervals, not just percentages.
- State whether the disagreement cohort is meaningfully and consistently worse, per the original acceptance criteria for this ticket.

## 5. Market-odds Section B

- Report whether market-odds testing (Section B) can now proceed using this data, and if so, what the results show. If this was never fully scoped, say so rather than guessing at what it should check.

## Reporting format

For each of items 2–5, give one of: VERIFIED / STILL BLOCKED (state on what) / PARTIALLY VERIFIED (state what's missing). Include row counts and raw numbers for everything — no rounded summaries without the underlying counts. Do not draw conclusions beyond what the data directly shows, and do not make any changes based on these results — this is a report-back only.
