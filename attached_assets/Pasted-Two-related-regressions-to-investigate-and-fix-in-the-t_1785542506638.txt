Two related regressions to investigate and fix in the tennis prediction app:
1. Upcoming Fixtures list on the Home page is empty (was previously populated).
In artifacts/tennis-predictor/src/components/FixturesList.tsx:
	•	First, temporarily log or render the existing diagnostics object (line ~410-423, already computes providerFixturesReceived, excludedDismissed, filteredByTourOrEvent, retained) so we can see exactly which stage is dropping fixtures to zero — the provider returning nothing, the tour/tournament filter excluding everything, or dismissedIds excluding everything.
	•	If excludedDismissed is at or near providerFixturesReceived, the bug is in isStaleFixture() (line 108): new Date(fixture.scheduledStart).getTime() < Date.now(). Check whether scheduledStart is being compared correctly across timezones — if it’s misinterpreting timezone offset, fixtures that are actually upcoming could be wrongly flagged “stale,” and if handleRemoveCancelled (line 336) was triggered (manually or automatically), it would mass-dismiss them into sessionStorage under dismissedFixtureIds.v1, where they’d stay hidden across reloads in the same session.
	•	Also check collectUpcomingWindow (artifacts/api-server/src/routes/upcomingWindow.ts) and the 5-minute TtlCache on ApiTennisProvider — confirm the cache isn’t silently serving a stale empty result and that no explicit-past-fixture filter was reintroduced there (there’s project history — .agents/memory/ttl-cache-silent-refresh-and-live-fixtures-filter.md — of this exact class of bug recurring).
	•	As an immediate fix regardless of root cause: add a manual “Clear dismissed fixtures” action (or auto-clear sessionStorage‘s dismissedFixtureIds.v1 on load if the resulting list is suspiciously empty while the provider reports fixtures available) so a bad dismissal state can’t permanently blank the page for a whole session.
2. Match predictions are showing “WIN PROBABILITY 100%” for every predicted winner, when they were previously showing realistic 50/50-range splits.
In artifacts/api-server/src/services/predictionEngine/simulator.ts, runMatchSimulation() runs 200 outer draws × 100 inner simulations with Gaussian jitter and should essentially never converge to an exact 100.0% mean win rate across varied matches. Since it’s happening consistently regardless of confidence tier or data quality:
	•	Log the actual player1ServicePointProbability / player2ServicePointProbability values going into runMatchSimulation for several real matches, and the jitterStdDev being applied. Confirm they’re not both collapsing to the same extreme clamp bound (0.3/0.92) or that estimate.reliability isn’t coming through as a degenerate value (e.g. always 0 or 100) that zeroes out jitter.
	•	Confirm deriveServicePointEstimate() (same file) is receiving real, varying surfaceElo and serveReturn inputs per match rather than a stale/cached/default value being reused across different matchups.
	•	Add a regression test asserting that runMatchSimulation output varies meaningfully across at least 5 different realistic input combinations, and that no single run produces exactly 100.0% or 0.0% unless the input probabilities are already at their extreme clamp bounds for both players simultaneously.
	•	Cross-check against calibratedProbability (used correctly elsewhere — the “too close to call” 49-53% cards render fine) to confirm the bug is isolated to the simulator path and not the ensemble/calibration pipeline.
Log before/after values for both fixes so we can visually confirm real fixtures reappear and simulated win probabilities show genuine variation across matches again.